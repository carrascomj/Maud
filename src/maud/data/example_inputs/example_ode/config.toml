name = "test-ode"
kinetic_model_file = "kinetic_model.toml"
priors_file = "priors.toml"
experiments_file = "experiments.toml"
likelihood = true
quench_timepoints = [0.1, 1.0, 10.0]

[cmdstanpy_config]
iter_warmup = 200
iter_sampling = 100
chains = 4
refresh = 1
adapt_init_phase = 20
metric = "dense_e"
adapt_metric_window = 5
